export enum OKRListTypesEnum {
    inProgress = 'inProgress',
    done = 'done', 
    cancel = 'cancel',
    byUser = 'byUser',
    byParent = 'byParent',
    byStrategy = 'byStrategy',
}

export enum OKRStatusItem {
    progress = 'в работе',
    done = 'выполнена',
    cancel = 'отменена',
}